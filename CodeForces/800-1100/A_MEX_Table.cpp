#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n, m; cin >> n >> m;
    cout << max(n, m)+1 << '\n';
}

int main() {
    int t; cin >> t;
    while (t--) {
        solve();
    }
}