#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long ull;
typedef long long ll;

void solve()
{
    int n, m; cin >> n >> m;
    vector<int> flowers(n), taking(m);
    for (int i=0;i<n;i++) cin >> flowers[i];
    for (int i=0;i<m;i++) cin >> taking[i];

    int noSkipPtr = 0, skipPtr = 1, curElm = 0;
    for (int i=0;i<n;i++) {
        
    }
}

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
}